# Jobs

Reserved for scheduled/background jobs (e.g. session cleanup, digest
emails) once a job queue (BullMQ on the Redis client already
configured in `config/redis.ts`) is introduced. Not used in Phase 4.
